<%= form_for(@budget) do |f| %>

  <div class="field">
    <%= f.label :debt_service %>
    <%= f.number_field :debt_service %>
  </div>

  <%= hidden_field_tag(:section, "debt_service") %>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
