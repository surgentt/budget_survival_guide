<h2> What do you spend on your commute? </h2>
<%= form_for(@budget) do |f| %>

  <div class="field">
    <%= f.label :commute %>
    <%= f.number_field :commute %>
  </div>

  <%= hidden_field_tag(:section, "commute") %>

  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
