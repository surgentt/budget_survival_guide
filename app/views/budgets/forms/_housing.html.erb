<h2>How much will you spend on housing a month? <br> (Rent, Mortgage, etc.) </h2>

<div class="row-fluid">
  <%= form_for(@budget) do |f| %>

    <div class="form-group col-md-3">
      <%= f.number_field :housing, class: "form-control", autofocus: true %>
    </div>

    <div class="row">
      <%= f.submit "Next", class: "btn btn-default" %>
    </div>

    <%= hidden_field_tag(:section, "housing") %>
  <% end %>
</div>
