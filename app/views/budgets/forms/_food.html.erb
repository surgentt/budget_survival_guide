<h2>How much do you plan on food a month?</h2>

<%= form_for(@budget) do |f| %>

  <div class="field">
    <%= f.number_field :food %>
  </div>

  <%= hidden_field_tag(:section, "food") %>

  <div>
    <%= f.submit "Next", class: "btn btn-default" %>
  </div>

<% end %>
